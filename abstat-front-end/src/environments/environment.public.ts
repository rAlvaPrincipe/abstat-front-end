// this is the "open" environment  for read only features and no authentication
export const environment = {
  production: true, //this is just a variable, it has no effect if not used
  mode: 'public'
};
