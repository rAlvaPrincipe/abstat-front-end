// this is the "full-closed" environment for full features with authentication
export const environment = {
  production: true, //this is just a variable, it has no effect if not used
  mode: 'full-closed'
};
