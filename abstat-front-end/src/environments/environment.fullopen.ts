// this is the "full-open" environment for full features without authentication
export const environment = {
  production: true, //this is just a variable, it has no effect if not used
  mode: 'full-open'
};
